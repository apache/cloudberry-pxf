dependencies {
    // PostgreSQL driver is used by PXF itself. 
    // Its version is defined in server/build.gradle

    // Clickhouse
    implementation('com.clickhouse:clickhouse-jdbc:0.9.7:all')                { transitive = false }

}