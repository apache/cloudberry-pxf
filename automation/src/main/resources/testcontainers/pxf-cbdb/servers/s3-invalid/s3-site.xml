<?xml version="1.0" encoding="UTF-8"?>
<!--
    Pre-baked PXF server 's3-invalid' configuration for testcontainers automation.

    Endpoint points at the MinIO sidecar, but the credentials are deliberately
    invalid. Used by CloudAccessTest to verify PXF credential-resolution
    precedence: a server config with bad keys should surface an authentication
    failure unless the external table provides overriding credentials (in
    which case the table-level creds should win).

    Shipped as-is and copied into ${PXF_BASE}/servers/s3-invalid/ by
    entrypoint.sh.
-->
<configuration>
    <property>
        <name>fs.s3a.endpoint</name>
        <value>http://minio:9000</value>
    </property>
    <property>
        <name>fs.s3a.access.key</name>
        <value>invalid-access-key</value>
    </property>
    <property>
        <name>fs.s3a.secret.key</name>
        <value>invalid-secret-key</value>
    </property>
    <property>
        <name>fs.s3a.path.style.access</name>
        <value>true</value>
    </property>
    <property>
        <name>fs.s3a.connection.ssl.enabled</name>
        <value>false</value>
    </property>
    <property>
        <name>fs.s3a.impl</name>
        <value>org.apache.hadoop.fs.s3a.S3AFileSystem</value>
    </property>
    <property>
        <name>fs.s3a.aws.credentials.provider</name>
        <value>org.apache.hadoop.fs.s3a.SimpleAWSCredentialsProvider</value>
    </property>
</configuration>
